# Builder Rep

This file is kept short intentionally. For full product context and design principles, see **CLAUDE.md**. For current project state and recommended next steps, see **HANDOFF.md**.

## Quick Start

```sh
cp .env.example .env.local
# Set OPENAI_API_KEY in .env.local
npm install
npm run dev
```

## Commands

- `npm run dev` — start development server
- `npm run build` — production build
- `npm run test` — run unit tests (vitest)
